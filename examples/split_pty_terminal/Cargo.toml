[package]
name = "split_pty_terminal"
version = "0.1.0"
edition = "2024"
publish = false
license = "Apache-2.0"

[dependencies]
gpui_ghostty_terminal = { path = "../../crates/gpui_ghostty_terminal" }
gpui = { workspace = true }
portable-pty = "0.9"
