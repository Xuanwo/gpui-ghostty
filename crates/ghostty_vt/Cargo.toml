[package]
name = "ghostty_vt"
version = "0.1.0"
edition = "2021"
publish = false

[features]
default = []
zig-build = ["ghostty_vt_sys/zig-build"]

[dependencies]
ghostty_vt_sys = { path = "../ghostty_vt_sys" }
